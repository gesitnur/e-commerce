<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
      <%= link_to 'E-Commerce', root_path, class: 'navbar-brand'%>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <% if user_signed_in? && current_user.has_role?(:vendor) %>
          <li class="nav-item">
            <%= link_to 'Produk', products_path, class: 'nav-link active' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Penjualan', sales_path, class: 'nav-link active' %>
          </li>
          <% end %>
          
          <% if user_signed_in? && current_user.has_role?(:admin) %>
          <li class="nav-item">
            <%= link_to 'User', user_index_path, class: 'nav-link active' %>
          </li>
          <% end %>

          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Kategori
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
              <li><%= link_to 'Semua Kategori' , root_path, class: 'dropdown-item' %></li>
              <% list_categories.each do |t| %>
              <li><%= link_to t.description , p_home_path(t.id), class: 'dropdown-item' %></li>
              <% end %>
            </ul>
          </li>
        </ul>
        <form class="d-flex">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <%= link_to 'Cart', carts_path, class: 'nav-link active' %>
          </li>
          <li class="nav-item">
            <a class="nav-link">Saldo : <%= user_signed_in? ? "Rp #{ number_with_precision( current_user.balance.balance, :precision => 0, :delimiter => '.') }": ''%></a>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <%= user_signed_in? ? current_user.name : 'Halo Guest'%>
              <%= %>
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                <% if user_signed_in?%>
                <li><%= link_to 'Transaksi',transaction_orders_path, class: 'dropdown-item' %></li>
                <% if user_signed_in? && current_user.has_role?(:customer) %>
                  <li><%= link_to 'Profile',transaction_orders_path, class: 'dropdown-item' %></li>
                <% end %>
                <li>
                </br>
                <li><%= link_to 'Logout', destroy_user_session_path, data: { "turbo-method": :delete}, class: 'dropdown-item' %></li>
                <li>
            </li>
    
                <%else%>
                <li><%= link_to 'Daftar', new_user_registration_path, class: 'dropdown-item'%></li>
                <li><%= link_to 'Login', new_user_session_path, class: 'dropdown-item'%></li>
                <%end%>
            </ul>
          </li>
        </ul>
        </form>
      </div>
    </div>
  </nav>